#12056.vs
run = "R -s -f cp_loop_002.R"

entrypoint = "cp_loop_002.R"

onBoot = """
[ -d "~/.config/R" ] || mkdir -p ~/.config/R
"""

[env]
R_LIBS_USER = "~/.config/R"

[packager]
language = "rlang"

[packager.features]
packageSearch = true