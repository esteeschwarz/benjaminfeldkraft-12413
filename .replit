run = "R -s -f 12042.chitest_essai.r"

entrypoint = "12042.chitest_essai.r"

onBoot = """
[ -d "~/.config/R" ] || mkdir -p ~/.config/R
"""

[env]
R_LIBS_USER = "~/.config/R"

[packager]
language = "rlang"

[packager.features]
packageSearch = true